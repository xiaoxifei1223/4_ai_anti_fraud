# 数据集下载指南

## ✅ 已下载完成

### 1. 图网络数据集 (YelpChi + Amazon)
- **状态**: ✅ 已下载
- **位置**: `data/graph/CARE-GNN/data/`
- **包含**:
  - YelpChi 数据集
  - Amazon 数据集
- **用途**: 测试 Neo4j 图网络分析、团伙欺诈识别

---

## 📥 需要手动下载

### 2. Kaggle Credit Card Fraud Detection (主数据集)

由于需要 Kaggle 账号和 API 配置，请按以下步骤手动下载：

#### 方法 1: 直接下载 (推荐)

1. **访问 Kaggle 数据集页面**:
   ```
   https://www.kaggle.com/datasets/mlg-ulb/creditcardfraud
   ```

2. **登录 Kaggle 账号**
   - 如果没有账号，需要先注册: https://www.kaggle.com/account/login

3. **下载数据集**
   - 点击页面右上角的 "Download" 按钮
   - 下载 `creditcard.csv` 文件 (约 150 MB)

4. **放置文件**
   - 创建目录: `data/raw/creditcard/`
   - 将下载的 `creditcard.csv` 放入该目录

#### 方法 2: 使用 Kaggle API (高级)

1. **安装 Kaggle API**:
   ```bash
   pip install kaggle
   ```

2. **获取 API Token**:
   - 访问: https://www.kaggle.com/settings
   - 找到 "API" 部分
   - 点击 "Create New API Token"
   - 下载 `kaggle.json` 文件

3. **配置 API Token**:
   - **Windows**: 将 `kaggle.json` 放到 `C:\Users\你的用户名\.kaggle\`
   - **Linux/Mac**: 将 `kaggle.json` 放到 `~/.kaggle/`
   - 设置权限 (Linux/Mac): `chmod 600 ~/.kaggle/kaggle.json`

4. **下载数据集**:
   ```bash
   # 方式 1: 使用我们的脚本
   python scripts/download_datasets.py
   
   # 方式 2: 直接使用 kaggle 命令
   kaggle datasets download -d mlg-ulb/creditcardfraud -p data/raw/creditcard/ --unzip
   ```

---

## 🎯 数据集说明

### Kaggle Credit Card Fraud Detection
- **文件**: `creditcard.csv`
- **规模**: 284,807 笔交易
- **欺诈率**: 0.172% (492 笔欺诈交易)
- **特征**:
  - `Time`: 交易时间 (秒)
  - `V1-V28`: PCA 转换后的匿名特征
  - `Amount`: 交易金额
  - `Class`: 标签 (0=正常, 1=欺诈)

### YelpChi Fraud Dataset
- **位置**: `data/graph/CARE-GNN/data/yelp/`
- **文件**:
  - `YelpChi.mat`: 主数据文件
  - 包含用户、评论、商家关系图
- **规模**: 45,954 个用户节点
- **欺诈率**: 8.37%

### Amazon Fraud Dataset
- **位置**: `data/graph/CARE-GNN/data/amazon/`
- **文件**:
  - `Amazon.mat`: 主数据文件
  - 多关系异构图
- **规模**: 11,944 个节点
- **欺诈率**: 9.5%

---

## 📂 期望的目录结构

下载完成后，目录结构应该如下：

```
data/
├── raw/
│   └── creditcard/
│       └── creditcard.csv          ← 需要手动下载
├── graph/
│   └── CARE-GNN/                   ✅ 已下载
│       └── data/
│           ├── yelp/
│           │   └── YelpChi.mat
│           └── amazon/
│               └── Amazon.mat
├── processed/                       (预处理后生成)
└── 下载指南.md                      (本文件)
```

---

## ✅ 验证下载

下载完成后，运行以下命令验证：

```bash
python scripts/verify_datasets.py
```

如果没有这个脚本，可以手动检查：

1. **检查 Credit Card 数据**:
   ```bash
   dir data\raw\creditcard\creditcard.csv
   ```
   应该看到一个约 150 MB 的文件

2. **检查图数据**:
   ```bash
   dir data\graph\CARE-GNN\data\yelp\
   dir data\graph\CARE-GNN\data\amazon\
   ```
   应该看到 `.mat` 文件

---

## 🚀 下一步

数据下载完成后：

1. **数据预处理**:
   ```bash
   python scripts/preprocess_data.py
   ```

2. **数据探索分析**:
   ```bash
   python scripts/analyze_data.py
   ```

3. **开始训练模型**:
   ```bash
   python scripts/train_model.py
   ```

---

## ❓ 常见问题

### Q: Kaggle API 配置失败?
A: 确保 `kaggle.json` 放在正确的位置，并且文件权限正确。

### Q: 下载速度很慢?
A: 可以使用代理或 VPN。也可以考虑从其他镜像站下载。

### Q: 图数据下载失败?
A: 确保安装了 Git，并且网络可以访问 GitHub。可以尝试使用 GitHub 镜像或手动下载 ZIP。

### Q: 数据集太大，磁盘空间不够?
A: 
- Credit Card 数据集: 约 150 MB
- 图数据集: 约 50 MB
- 总计约 200 MB，应该足够

---

## 📞 需要帮助?

如果遇到问题，请检查：
1. 网络连接
2. Kaggle 账号权限
3. Git 安装
4. 磁盘空间

或者直接从备用源下载（如果有的话）。
